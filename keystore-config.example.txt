# Keystore Configuration File
# Copy this file to keystore-config.txt and fill in your values
# DO NOT commit keystore-config.txt to Git!

KEYSTORE_PASSWORD=your-keystore-password-here
KEY_PASSWORD=your-key-password-here
KEYSTORE_NAME=Your Name or Organization Name
KEYSTORE_ORG=PandaGarde
KEYSTORE_ORG_UNIT=
KEYSTORE_CITY=Your City
KEYSTORE_STATE=Your State or Province
KEYSTORE_COUNTRY=US

# Notes:
# - KEYSTORE_PASSWORD: Main password for the keystore file (required)
# - KEY_PASSWORD: Password for the key alias (can be same as KEYSTORE_PASSWORD)
# - KEYSTORE_NAME: Your name or organization name (required)
# - KEYSTORE_ORG: Organization name (defaults to PandaGarde if not set)
# - KEYSTORE_ORG_UNIT: Organizational unit (optional)
# - KEYSTORE_CITY: Your city (required)
# - KEYSTORE_STATE: Your state or province (required)
# - KEYSTORE_COUNTRY: Two-letter country code, e.g., US, CA, GB (defaults to US)

